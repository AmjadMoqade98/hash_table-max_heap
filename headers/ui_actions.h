#include "hash_table.h"

void insert_record_to_table(table_entry* table);
void delete_record_from_table(table_entry* table);
void search_for_name(table_entry* table);
void update_year_freq_record(table_entry* table);
void add_year_freq_record(table_entry* table);
void read_names_from_file(table_entry* table);
